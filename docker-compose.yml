version: '3.8'

services:
  web:
    image: 3d_human_body_shape:1
    # build:
    #   context: .
    #   dockerfile: Dockerfile
    ports:
      - "8080:8080"
    volumes:
      - ./server.py:/app/server.py
      # - ./src:/app/src
    # environment:
      # FLASK_ENV: development
